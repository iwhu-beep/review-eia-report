---
name: review-eia-report
version: 2.0.0
license: MIT
description: 审核建设项目环境影响报告表（环评报告表），检查报告中的常见问题和错误，输出结构化审核意见清单。支持读取 Word 文档(.docx/.doc)、PDF 或粘贴的文本内容。当用户提到环评、环境影响报告、报告表审核、环保审查、三线一单、总量控制时使用此技能。
triggers:
  - 环评
  - 环境影响报告
  - 报告表审核
  - 环保审查
  - 三线一单
  - 总量控制
compat:
  - qoder
  - claude-code
  - codex
  - opencode
---

# 审核环境影响报告表

## 使用场景

用户提供建设项目环境影响报告表（Word / PDF / 文本），逐项审核并输出审核意见清单。
适用于：环评编制自查、第三方技术审查、审批前预审、退件整改复核。

非适用：报告书（需用导则全项评价）、规划环评、验收调查——可复用本清单逻辑，但须按对应导则补项。

## 工作流程

### 第 0 步：预检分流（先分类，再审核）

1. **文件类型与扫描预检**：`.docx` / `.doc` / `.pdf` / 粘贴文本。`docx` 优先用脚本提取；`doc` 先转 `docx`；`pdf` 必须先跑预检：
   ```bash
   python scripts/extract_docx.py 报告.pdf --scan-check   # 退出码4=疑似扫描件
   ```
   疑似扫描件（平均每页不足50字符）→ 提醒用户先 OCR（如 ocrmypdf / WPS 文字识别）后再审，禁止基于残缺文本下结论；文本型 PDF 才进入提取。
2. **报告类别判定（按环办环评〔2020〕33号）**：先定用哪套表——以污染影响为主→污染影响类；以生态影响为主、或同时涉及污染+生态→生态影响类（用错表即重大，见 checklist 第零节）。
   专项预判：对照指南表1（大气/地表水/环境风险/生态/海洋），预判应设专项；土壤、声不设专项，地下水原则不设（涉集中式饮用水源/特殊地下水资源除外）；专项一般不超过两项。
5. **污染/生态分支**：污染影响类→本流程全量执行；生态影响类→本 skill 仅复用清单通用逻辑（基本信息/一致性/编制质量），生态调查、生态影响预测、生态保护措施改按《生态影响类编制技术指南（试行）》补查，并在审核意见中注明"生态类专项内容按生态指南核查"。混合疑义（分不清污染/生态为主）→ 先定类再审，不得混用两套口径。
3. **建设性质分流**：
   - 新建 → 跳过"以新带老""三本账"检查；
   - 改扩建 / 技改 → 必须检查现有工程、"以新带老"、改扩建前后"三本账"。
4. **行业分流**：化工、电镀、印染、制革、铸造、养殖、喷涂等先行对照 `checklist.md` 第十一节行业附录。

### 第 1 步：获取报告内容

**如果用户提供文件路径：**

1. 安装依赖：`pip install -r scripts/requirements.txt`
2. 运行提取脚本（仓库根目录执行，相对路径）：

```bash
# Markdown 输出（默认，向后兼容）
python scripts/extract_docx.py "<文件路径>"

# JSON 输出（供程序化校验/二次开发）
python scripts/extract_docx.py "<文件路径>" --format json --out extracted.json

# 超长文档：限制单次输出字符数，配合分块审核
python scripts/extract_docx.py "<文件路径>" --max-chars 60000
```

**标准时效初筛（联网核验前置）：**

```bash
# 从提取文本中自动识别标准号，对照本地库判现行/过期/未知
python scripts/verify_standards.py extracted.md --format md --out std_check.md
python scripts/verify_standards.py extracted.md --format json --out std_check.json
```

脚本只做初筛；`status=未知/疑似过期` 的条目必须到官网复核（见第 4 步第 7 项）。

3. 读取输出内容进入审核流程。脚本输出包含：文档元数据、正文分区、表格分区、页眉页脚。

**如果用户粘贴文本：** 直接进入审核流程，但需先确认是否缺附图附表（缺图则在"编制质量"中记一般问题）。

**长文档处理（超上下文时强制分块）：**

1. 先提取"关键字段摘要"（见第 2 步）一次性载入；
2. 正文按章节分块审核（工程分析 / 现状 / 预测 / 措施 / 总量 / 结论），表格块单独完整保留不截断；
3. 每块输出暂存问题清单，最后合并去重并做交叉验证（第 4 步）。

### 第 2 步：通读全文，提取关键信息

先输出一张**关键信息表**（后文审核全部引用它，前后矛盾即记问题）：

| 字段 | 提取值 | 位置 |
|------|--------|------|
| 项目名称 / 建设单位 / 法人 | | 封面+营业执照附件 |
| 建设地点（精确到门牌/坐标） | | |
| 建设性质（新建/改扩建/技改） | | |
| 行业类别及代码（GB/T 4754-2017含第1号修改单） | | |
| 总投资 / 环保投资 / 占比 | | |
| 占地 / 建筑面积 / 投产日期 | | |
| 原辅材料（名称+年用量+危化品标注） | | |
| 主要设备（型号+数量+功率+产能/能耗匹配性） | | 缺功率记"报告未给出" |
| 工艺流程+产污节点 | | |
| 污染源（废气/废水/噪声/固废四类） | | |
| 污染因子清单（分介质列全，分项见第4步第6项） | | 缺项记"报告未给出" |
| 引用排放标准（标准号+年号） | | |
| 总量指标（COD/氨氮/SO2/NOx/VOCs） | | |

> 提取不到的字段不要脑补，记为"报告未给出"，并在第 3 步中按缺失项判定等级。

### 第 3 步：逐项审核

按以下 10 个维度逐项审核，详细检查清单见 [checklist.md](checklist.md)（★为必查，⛔为一票否决）：

| 序号 | 审核维度 | 权重 | 重点关注 |
|------|----------|------|----------|
| 1 | 项目基本信息完整性 | 高 | 必填字段齐全、前后一致 |
| 2 | 工程分析 | 高 | 建设内容、规模、工艺、产污环节、物料平衡 |
| 3 | 环境质量现状评价 | 中 | 数据来源、监测时效（≤3年）、评价标准 |
| 4 | 环境影响预测与评价 | 高 | 预测方法、参数合理性、结论可信度 |
| 5 | 环保措施可行性 | 高 | 技术可行、经济合理、达标论证、参数完整 |
| 6 | 总量控制指标 | 高 | 指标来源、计算依据、区域平衡、排污许可衔接 |
| 7 | 公众参与 | 低 | 报告表一般不要求，有要求时查合规性 |
| 8 | 法规政策符合性 | 高 | 产业政策、选址规划、生态红线、"三线一单" |
| 9 | 数据一致性与逻辑性 | 高 | 表间一致、独立验算、结论与正文一致 |
| 10 | 编制质量 | 中 | 标准时效、文字规范、图表完整 |

#### 3.0 指南结构符合性（版式先行，依据《污染影响类编制技术指南（试行）》）

先对版式再查内容，明细见 [checklist.md](checklist.md) 第零节：

1. **章节齐全性**：一、基本情况 → 二、工程分析 → 三、区域现状+目标+标准 → 四、影响和措施 → 五、监督检查清单 → 六、结论 → 附图附件，缺章即记一般（缺"监督检查清单/结论"记较大）；
2. **专项设置判定**（指南表1）：大气（有毒有害名录污染物+厂界外500m有保护目标）、地表水（新增工业废水直排）、环境风险（Q≥1按HJ 169）、生态（新增河道取水+重要水生生物栖息地）、海洋（直接向海排放）。该设未设记重大；不该设乱设（>2项）记一般；
3. **简化要求负面清单**（指南已取消项，不得作为问题提出）：评价等级判定、模型预测、环保措施技术经济论证、超范围现状监测（声50m/大气500m/地下水500m范围外硬要数据）。未设专项的要素只查定性分析+源强+达标；
4. **源强与可行技术**：源强核算是否按源强核算技术指南+排污许可技术规范；治理设施非可行技术是否分析可行性；废水间接排放是否论证依托污水厂（能力/工艺/进出水）；开停车等非正常工况是否分析。

审核顺序建议：8 → 1 → 2 → 9 → 4 → 5 → 6 → 3 → 10 → 7（先判"能不能建"，再查"算得对不对"）。

### 第 4 步：交叉验证（强制复核，防漏判）

输出前必须执行以下 8 项验算，不通过即升级严重程度：

1. **排放量验算**：`E(t/a) = C(mg/m³) × Q(m³/h) × T(h/a) × 1e-9`，偏差 >10% 记一般，>30% 记较大；
2. **三表勾稽**：工程分析表 = 污染物排放汇总表 = 总量控制表，三处同一污染物年排放量必须一致；
3. **物料平衡**：投入 ≈ 产品 + 损耗 + 废物，不闭合记一般，差 >20% 记较大；
4. **结论一致性**：正文分析 ↔ 环评结论 ↔ 总量指标三者矛盾，直接记重大；
5. **标准年号三核对**：标准号 + 年号 + 限值逐一对照 [regulations.md](regulations.md)，引用过期标准且影响达标判断记重大，否则记较大（详见法规文件第十节三步法+第十二节联网核验）。
6. **污染因子防遗漏核查（五步倒查，必做）**：按 `checklist.md` 第十一节行业因子矩阵逐项勾选——
   ① 原辅材料倒查（每个组分→燃烧/挥发/反应产物）、② 工艺节点倒查（每工序四类产污全覆盖）、
   ③ 标准反查（引用的排放标准管控因子必须查全）、④ 三表勾稽（同因子三表同名同量）、
   ⑤ 行业矩阵对照（命中行业时必查★因子+易漏因子）。
   判定：必查★因子缺失记较大（致结论不可信升级重大）；易漏因子缺失记一般（≥2项升级较大）；同因子前后名称不一致记一般。
7. **标准时效联网核验（必做）**：先跑 `python scripts/verify_standards.py <提取文本> --format md` 得初筛表，再对其中`疑似过期/未知/缺年号`条目逐条官网复核——GB类查国家标准全文公开系统（openstd.samr.gov.cn），HJ/环境标准查生态环境部标准栏目，查到废止/替代即按"标准年号三核对"升级判定。复核结论写入审核意见"判定依据"栏，禁止只贴脚本结果不复核。
8. **设备防遗漏核查（三对照，必做）**：按 `checklist.md` 第二节三对照+第十一节设备矩阵逐项勾选——
   ① 工艺→设备（每工序有对应设备）、② 产能→设备（单机产能×数量覆盖申报产能）、
   ③ 能耗→设备（装机功率与用电量匹配）+辅助公用与环保设备配齐。
   判定：核心生产设备缺失记重大；关键辅机缺失记一般（影响达标升级较大）。

### 第 5 步：输出审核意见

同时输出 Markdown（给人读）+ JSON（给程序用）。默认先给 Markdown，用户要对接系统时再给 JSON。
需存档/送审时，最后导出 PDF：

```bash
# 先把审核意见存为 md，再转 PDF（中文字体自动发现，特殊符号自动转文字）
python scripts/export_pdf.py 审核意见.md --out 审核意见.pdf
```

**Markdown 模板：**

```markdown
# 环境影响报告表审核意见

## 项目信息
- 项目名称：[名称]
- 建设单位：[单位]
- 建设地点：[地点]
- 建设性质：[新建/改扩建/技改]
- 行业类别：[类别+代码]
- 审核日期：[日期]
- 报告版本：[送审稿/报批稿，如已知]

## 审核结论摘要
- 重大缺陷：X 项（一票否决 Y 项）
- 较大缺陷：X 项
- 一般缺陷：X 项
- 瑕疵：X 项
- 总评结论：不通过 / 有条件通过 / 基本通过 / 通过（四选一，判定规则见缺陷分级矩阵）

## 详细审核意见

### 一、[审核维度名称][权重：高]

#### [问题 1][⛔ 一票否决]
- 问题描述：[具体描述]
- 所在位置：[章节/页码/表格编号]
- 缺陷等级：重大 / 较大 / 一般 / 瑕疵
- 判定依据：[checklist条目编号/法规标准号]
- 修改建议：[具体、可执行的建议]

## 整改优先级
1. 重大缺陷（否决性，不改必退件）：...
2. 较大缺陷（影响审批，限期整改）：...
3. 一般缺陷（补充说明）：...
4. 瑕疵（文字格式，顺手改）：...

## 总结
[整体评价及重点整改建议，不超过200字]

## 风险声明
[本审核意见由 AI 辅助生成，仅供技术审查参考，不能替代注册环评工程师判断和法定审批；全部意见须经人工复核确认；法规标准状态以官网现行版本为准。导出 PDF 时该声明自动附带。]
```

**JSON Schema（精简版）：**

```json
{
  "project_info": {"name": "", "company": "", "location": "", "type": ""},
  "summary": {"fatal": 0, "major": 0, "minor": 0, "cosmetic": 0, "verdict": "fail|conditional|pass_with_notes|pass"},
  "issues": [
    {
      "id": "8-01",
      "dimension": "法规政策符合性",
      "description": "",
      "location": "",
      "severity": "fatal|major|minor|cosmetic",
      "veto": false,
      "basis": "checklist条目/标准号",
      "suggestion": ""
    }
  ],
  "disclaimer": "AI辅助生成，须经人工复核确认"
}
```

### 缺陷分级与总评结论矩阵

| 等级 | 判定标准 | 典型情形 | 总评挂钩 |
|------|----------|----------|----------|
| **重大 fatal** | 违反强制性要求 / 结论性数据错误 / 否决性缺失；含全部 ⛔ 一票否决项 | 生态红线/水源地未论证；总量缺失；结论与正文矛盾；废止标准影响达标判断；三本账缺失（改扩建）；核心设备缺失 | 存在重大→**不通过**：重大整改后重新送审 |
| **较大 major** | 关键内容缺失 / 数据严重错误 / 需限期整改 | 必查★因子遗漏致结论存疑；三表大偏差；处理效率无依据；以新带老缺失 | 无重大、有较大→**有条件通过**：限期整改并复核 |
| **一般 minor** | 内容不完整、数据不充分、需补充说明 | 监测数据超3年；附图缺失；环保投资明细缺失；易漏因子缺失 | 仅一般→**基本通过**：完善后报批 |
| **瑕疵 cosmetic** | 文字格式、可优化的表述 | 术语不规范；编号跳号；错别字 | 仅瑕疵→**通过** |

计数规则：同一根因在多处出现只计 1 项，但在位置栏列出全部出现处；重大项单独计数并在摘要中注明。
旧版映射：严重→重大（⛔/强制性/结论矛盾）或较大（其余）；一般→一般；建议→瑕疵。

## 注意事项

- 以**现行有效**国家标准和法规为准，标准核查用"标准号+年号+限值"三核对法（见 regulations.md 第十节）；
- 对数据计算必须独立验算，禁止直接采信报告结论；
- "环评结论"与正文分析不一致，一律记**重大**；
- 改扩建项目无"以新带老"或无"三本账"，一律记**重大**；
- 报告表一般不要求公众参与，不得以"无公参"为由扣分；地方另有要求时才按 checklist 第七节核查；
- 不确定的法规状态写"建议复核"，不要编造实施日期。
- 2026-08-15后新编报告引用10部已废止单行法（环保/环评/大气水土固废噪声等）须更新为《生态环境法典》对应条款并核对新编号（过渡规则见 regulations.md 第一节）。
- **强风险声明**：本 skill 为 AI 辅助审核，不能替代注册环评工程师的专业判断和法定审批程序；所有输出意见（含缺陷分级与总评结论）必须经人工复核确认后方可作为整改或报批依据；标准时效以官网现行版本为准。

## 参考资料

- 详细审核检查清单（含权重/必查/验算公式）：[checklist.md](checklist.md)
- 法规政策参考（附现行状态/实施日期）：[regulations.md](regulations.md)
- 文本提取脚本：`scripts/extract_docx.py`，依赖见 `scripts/requirements.txt`
- 标准时效初筛：`scripts/verify_standards.py` + 本地库 `scripts/standards_db.json`（官网复核方法见 [regulations.md](regulations.md) 第十二节）
- 审核意见导出 PDF：`scripts/export_pdf.py`（依赖 fpdf2，中文字体自动发现）
- 变更记录：[CHANGELOG.md](CHANGELOG.md)
